import type { RestrictedSyntax } from './type.js';

/**
 * Default restricted syntax used by the ESLint rule no-restricted-syntax.
 *
 * @see https://eslint.org/docs/latest/rules/no-restricted-syntax
 */
export const restrictedSyntax: Readonly<RestrictedSyntax> = Object.freeze([
  // List of restricted syntax

]);
